// este archivo es la representacion clara
// de lo que tiene un producto
// De esta forma podemos tipar los productos
export interface Product {
    id: string;
    image: string;
    title: string;
    price: number;
    description: string;
}
